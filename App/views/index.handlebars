<h1>UNDEFINED!</h1>

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h2>Burgers Avaiable Now!</h2>
      <ul>
        {{#each cats}}
          {{#unless eaten}}
            {{> cats/burger-block eaten=true}}
          {{/unless}}
        {{/each}}
      </ul>
    </div>
      <div class="col-md-6">
        <h2>Previous Burgers</h2>
    `    <ul>
          {{#each cats}}
            {{#if eaten}}
              {{> cats/burger-block eaten=false}}
            {{/if}}
          {{/each}}
        </ul>
      </div>

  </div>
</div>
<h2>Order A Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>



  <button type="submit">Confirm</button>
</form>
